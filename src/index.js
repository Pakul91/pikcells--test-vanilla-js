import { state } from "./state";
const designContainer = document.querySelector(".design-selection-container");
const layers = document.querySelectorAll(".layer");

function populateLayers() {
  console.log(state);
}

populateLayers();
